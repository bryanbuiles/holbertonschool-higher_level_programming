"""
    Mylist module test


"""



>>> MyList = __import__('1-my_list').MyList

Basic usage:
    >>> my_list = MyList()
    >>> my_list.print_sorted()
    []
    >>> my_list.append(-2)
    >>> my_list.append(5)
    >>> my_list.append(2)
    >>> my_list.append(0)
    >>> print(my_list)
    [-2, 5, 2, 0]

Sorted list:
    >>> my_list.print_sorted()
    [-2, 0, 2, 5]

Big int in MyList:
    >>> my_list = MyList()
    >>> my_list.append(9999999999999)
    >>> my_list.append(-2)
    >>> my_list.append(0)
    >>> print(my_list)
    [9999999999999, -2, 0]

Big int print_sorted:
    >>> my_list.print_sorted()
    [-2, 0, 9999999999999]

just one int in MyList:
    >>> my_list = MyList()
    >>> my_list.append(8)
    >>> print(my_list)
    [8]

just one int in MyList sorted:
    >>> my_list.print_sorted()
    [8]

only negative int:
    >>> my_list = MyList()
    >>> my_list.append(-2)
    >>> my_list.append(-5)
    >>> my_list.append(-3)
    >>> my_list.append(-1)
    >>> print(my_list)
    [-2, -5, -3, -1]

Sorted list:
    >>> my_list.print_sorted()
    [-5, -3, -2, -1]

Test clear:
    >>> my_list = MyList([1, 2, 20])
    >>> my_list.clear()
    >>> print(my_list)
    []
